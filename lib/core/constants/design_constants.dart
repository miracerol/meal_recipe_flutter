import 'package:flutter/cupertino.dart';

class DesignConstants {
  static var searchRadius = BorderRadius.circular(15);
  static var homeCardBorderRadius = BorderRadius.circular(10);
  static var listCardBorderRadius = BorderRadius.circular(10);

  static const verySmallPaddingAll = EdgeInsets.all(1);
  static const mediumPaddingAll = EdgeInsets.all(10);
  static const largePaddingAll = EdgeInsets.all(15);

  static const mediumPaddingVertical = EdgeInsets.only(bottom: 10.0, top: 5.0);

  static const largePaddingLeft = EdgeInsets.only(left: 15.0);
  static const largePaddingRight = EdgeInsets.only(right: 15.0);
  static const mediumPaddingBottom = EdgeInsets.only(bottom: 10.0);

  static const themeDuration = Duration(seconds: 1);
  static const notFoundDuration = Duration(seconds: 2);
}
